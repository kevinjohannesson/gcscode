# Roadmap and project-status doc — implementation plan

_Note: The term "plugin" was renamed to "extension" in [ADR-0004](../decisions/ADR-0004-rename-plugin-to-extension.md). This document records the original terminology._

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a single-page `docs/roadmap.md` covering phase-plan status (A/B/C with sub-iterations) plus first-party feature plugins (Coming/Considering), and a `CLAUDE.md` pointer making the roadmap discoverable as the start-here doc.

**Architecture:** Pure docs change — one new file (`docs/roadmap.md`, content quoted verbatim in the spec) plus one bullet added to the start of `CLAUDE.md` `## Further reading`. No code, no tests, no plugin-API change. Lands as a single commit on master (no feature branch, per CLAUDE.md "Spec/plan commits can land on master directly" and per the precedent commit `e7d5692 docs: codify A1+A2-learned conventions in CLAUDE.md`).

**Tech Stack:** Markdown. Validated by `pnpm format` (Prettier on `.md` files) and `pnpm lint` (ESLint + `prettier --check`). No test runner involved in the change itself; existing tests run only as a sanity check.

**Spec:** `docs/specs/2026-04-27-roadmap.md`

---

## File structure

| Path              | Responsibility                                                                                                     |
| ----------------- | ------------------------------------------------------------------------------------------------------------------ |
| `docs/roadmap.md` | NEW. Single-page status surface — phase plan + feature plugins + maintenance protocol. ~55 lines. (Task 2 Step 1.) |
| `CLAUDE.md`       | One bullet added at the start of the `## Further reading` list pointing at the new roadmap. (Task 2 Step 2.)       |

---

### Task 1: Establish green baseline

**Files:** none

- [ ] **Step 1: Verify on master with clean working tree**

Run: `git status && git branch --show-current`
Expected: `nothing to commit, working tree clean`. Branch is `master`. (This iteration commits directly to master — no feature branch, per the spec's `## Branching and commit` section.)

- [ ] **Step 2: Verify lint clean**

Run: `pnpm lint`
Expected: ESLint passes; `Checking formatting...` followed by `All matched files use Prettier code style!`. Exit code 0.

(Skip `pnpm test` and `pnpm check` at baseline — this iteration touches no code or test files; sanity-check both at the end of Task 3 instead.)

---

### Task 2: Create `docs/roadmap.md` and add the `CLAUDE.md` pointer (single commit)

**Files:**

- Create: `docs/roadmap.md`
- Modify: `CLAUDE.md`

This task creates both files and lands a single commit. There is no failing-test-first cycle (this is docs).

- [ ] **Step 1: Create `docs/roadmap.md` with the spec's quoted content**

Create the file `docs/roadmap.md` at the repo root (i.e. `<workspace>/docs/roadmap.md`) with exactly this content:

```md
# Roadmap

Single-page status: where the gcscode plugin architecture is and what's planned. Iteration detail lives in `docs/specs/`, plans in `docs/plans/`, rationale in `docs/decisions/`, deferral detail (organized by topic) in `docs/out-of-scope.md`. This file is the start-here "where are we now / what's next" view.

## Phase plan

The plugin architecture grows in phases: **A** (contribution kinds), **B** (lifecycle and cleanup), **C** (cross-cutting capabilities). Each phase contains numbered iterations.

### Phase A — Contribution kinds

- [x] **A1: Status bar item** — `host.registerStatusBarItem` + footer rendering. Spec: [`specs/2026-04-26-phase-a1-status-bar.md`](specs/2026-04-26-phase-a1-status-bar.md)
- [x] **A2: Commands** — `host.registerCommand` + `executeCommand` (host- and registry-side). Spec: [`specs/2026-04-26-phase-a2-commands.md`](specs/2026-04-26-phase-a2-commands.md)
- [x] **A3: Keybindings** — `host.registerKeybinding` + a shell-level keyboard dispatcher. Spec: [`specs/2026-04-26-phase-a3-keybindings.md`](specs/2026-04-26-phase-a3-keybindings.md)
- [ ] **A4+: more contribution kinds** — menus, palette entries, sidebar tree views all candidates. Trigger: a real consumer needs the surface. See [`out-of-scope.md`](out-of-scope.md).

### Phase B — Lifecycle and cleanup

- [x] **B1: Deactivate orchestration** — `registry.deactivate(pluginId)` iterates subscriptions LIFO with error resilience. Spec: [`specs/2026-04-26-phase-b1-deactivate-orchestration.md`](specs/2026-04-26-phase-b1-deactivate-orchestration.md)
- [ ] **B2: Plugin enable/disable + reactive plumbing** — adds runtime `enabled` state per plugin and the reactive plumbing so mounted UI reflects state changes. Trigger: a "disable plugin" UI or any visible state-change need.
- [ ] **B3: Dev-time hot module reload** — Vite HMR boundary that re-imports a plugin module on edit and replays activate. Trigger: plugin-author iteration friction.
- [ ] **`Plugin.deactivate?()` hook** — optional plugin-side hook for non-disposable / async cleanup. Split off from B1 by design. Trigger: first plugin needing it (named on-deck consumer: SITL listener — see Feature plugins below).

### Phase C — Cross-cutting capabilities

- [ ] **Phase C scope** — TBD. ADR-0003 sketches host namespacing (`host.commands.register(...)`) once the flat surface exceeds 5–7 methods, plus events, settings, themes, and i18n as real consumers pull on them. Re-scope when a feature plugin pulls on it.

## Feature plugins

The first-party plugins planned for the app. Each is a future consumer of the plugin architecture; some are named triggers for deferred phase work.

### Coming (committed — will ship)

- [ ] **SITL listener** — software-in-the-loop data-feed listener. First consumer of `Plugin.deactivate?()` hook (will hold a connection that needs explicit close); likely first to want a Phase C streaming/connection service.
- [ ] **Map** — geographical view + selection state. Likely fits the existing view contribution kind; may surface a need for shared map state.
- [ ] **Video feed** — live video stream display. Likely a Phase C streaming-source consumer alongside SITL.

### Considering (not yet committed)

- [ ] **Road scanning** — _description TBD_

## Maintenance

This doc is updated alongside other per-iteration docs propagation (`out-of-scope.md`, ADR-0003 retrospective):

- **Iteration ships** → flip its checkbox, link the spec, and (optionally) reference the merge SHA.
- **Iteration deferred or rescoped** → update its line + trigger, leave checkbox unchecked.
- **New deferral surfaces during a spec** → add an unchecked item with its trigger.
- **User mentions a feature idea offhand during planning chat** → propose adding it here. Default to **Considering**; escalate to **Coming** when the user signals commitment ("we'll need", "100% in", "definitely going to be").

The doc is a manually-maintained list, not a generated artifact. When in doubt about whether something belongs here, prefer the lighter touch — a one-line entry that links somewhere richer is better than re-stating that richer source.
```

- [ ] **Step 2: Edit `CLAUDE.md` to add the roadmap bullet at the start of `## Further reading`**

Find the existing section in `CLAUDE.md` that begins:

```md
## Further reading

- `docs/out-of-scope.md` — canonical list of what is intentionally NOT built yet. Check here before building anything new.
- `docs/decisions/` — architecture decision records.
- `packages/plugin-api/README.md` — how to write a plugin.
- `packages/plugin-example/README.md` — the worked example to mirror.
```

Insert the new bullet immediately after the blank line following the `## Further reading` heading and BEFORE the existing first bullet, so the section becomes:

```md
## Further reading

- `docs/roadmap.md` — phase plan + iteration status + planned feature plugins. Start here for "where are we now / what's next".
- `docs/out-of-scope.md` — canonical list of what is intentionally NOT built yet. Check here before building anything new.
- `docs/decisions/` — architecture decision records.
- `packages/plugin-api/README.md` — how to write a plugin.
- `packages/plugin-example/README.md` — the worked example to mirror.
```

Don't modify the existing four bullets — only add the one new bullet at the front of the list. Don't change anything else in `CLAUDE.md`.

- [ ] **Step 3: Run format and lint**

Run: `pnpm format && pnpm lint`
Expected: Prettier rewrites any non-canonical formatting (likely a no-op for content authored to spec-quoted form, but possible if line lengths or list spacing diverge); `pnpm lint` then reports `All matched files use Prettier code style!` and exits 0.

If `pnpm lint` flags anything other than the two files this task touched, stop and investigate before staging — `pnpm format` should only have rewritten the two files this task touched (or none of them).

- [ ] **Step 4: Verify only the intended files are pending changes**

Run: `git status`
Expected: untracked file `docs/roadmap.md`; modified file `CLAUDE.md`. No other files changed.

If any other file appears in the output, stop and investigate before proceeding — the change should be exactly two files.

- [ ] **Step 5: Commit**

```bash
git add docs/roadmap.md CLAUDE.md
git commit -m "$(cat <<'EOF'
docs: add docs/roadmap.md + CLAUDE.md pointer

Creates docs/roadmap.md as a single-page "where are we now / what's
next" surface, consolidating phase-plan + iteration status + planned
feature plugins (Coming and Considering) into one discoverable doc.
Replaces the assemble-from-pieces status story (agent memory + ADR-0003
follow-ups + spec/plan filename pattern) with a single source of truth.

Maintenance protocol in the roadmap covers the per-iteration update
rule plus the new "user mentions an idea in chat → propose adding here"
rule so chat-mentioned features don't decay.

CLAUDE.md gains one bullet at the start of ## Further reading pointing
at the new roadmap as the start-here doc.

Docs-only iteration; lands on master directly per CLAUDE.md and per the
precedent commit e7d5692 (docs: codify A1+A2-learned conventions in
CLAUDE.md).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 6: Verify commit landed**

Run: `git log --oneline -3 && git status`
Expected: the new commit at HEAD with subject `docs: add docs/roadmap.md + CLAUDE.md pointer`; the two commits before it are this iteration's plan commit (`docs: implementation plan for roadmap and project-status doc`) and the spec commit (`89a485c docs: spec for roadmap and project-status doc`); `nothing to commit, working tree clean`.

---

### Task 3: End-to-end verification

**Files:** none

- [ ] **Step 1: Re-verify lint clean**

Run: `pnpm lint`
Expected: clean.

- [ ] **Step 2: Re-verify check (sanity — should be unchanged since no code touched)**

Run: `pnpm check`
Expected: clean across all three packages — same baseline state as Task 1.

- [ ] **Step 3: Re-verify tests pass (sanity — should be unchanged)**

Run: `pnpm test`
Expected: 76 in `@gcscode/shell`, 3 in `@gcscode/plugin-example` — 79 tests passing total. Same baseline as Task 1.

- [ ] **Step 4: Manual link-target check on `docs/roadmap.md`**

Confirm each spec/plan/out-of-scope link resolves to an existing file:

```bash
ls docs/specs/2026-04-26-phase-a1-status-bar.md \
   docs/specs/2026-04-26-phase-a2-commands.md \
   docs/specs/2026-04-26-phase-a3-keybindings.md \
   docs/specs/2026-04-26-phase-b1-deactivate-orchestration.md \
   docs/out-of-scope.md
```

Expected: all five paths listed without "No such file or directory" errors. (These are the five external paths referenced by the new `docs/roadmap.md`.)

- [ ] **Step 5: Manual visual check (optional, only if a markdown renderer is available)**

If a markdown renderer is available (a browser viewing github.com, VS Code's preview, or chrome-devtools-mcp pointed at a markdown server), open `docs/roadmap.md` and confirm:

- The four shipped iterations (A1, A2, A3, B1) render as filled checkboxes.
- The deferred / planned items render as empty checkboxes.
- The spec links are clickable and resolve.

If no renderer is available, skip — the syntax is plain text and renders correctly on github.com by default.

---

## Out of scope reminders

These are intentionally NOT part of this iteration (see the spec's `## Non-goals`):

- Duplicating `docs/out-of-scope.md`'s topic-by-topic deferral list.
- Duplicating ADR rationale.
- A status line at the top of `CLAUDE.md`.
- Tooling / CI / docs-site as parallel tracks in the roadmap.
- Automated generation of any roadmap content.
- Cross-link from `out-of-scope.md` back to `roadmap.md` (deferred follow-up; can land alongside the next out-of-scope edit).

If a step tempts you toward any of those, stop and re-read the spec.

## Cross-cutting note

This is a docs-only iteration with no test surface, so the plan deliberately omits TDD steps (no failing-test-first cycle) and the feature-branch + per-task reviewer machinery used by code iterations. It mirrors the precedent of `e7d5692` (the prior docs-only commit that codified A1+A2-learned `CLAUDE.md` conventions). Future maintenance updates to `docs/roadmap.md` happen alongside their owning iteration's docs commit, not as standalone iterations of their own.
